#pragma once
#include <string>
#include <iostream>
#include "board.hpp"


extern int ChangeCharToInt(std::string row);
extern void OutputBoard(Board bored);
extern int ChangeCharIntToRealInt(std::string number);
extern int ChangeStringToDirection(std::string direction);
